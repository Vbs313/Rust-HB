# 完整代码骨架搭建与首次构建验证

> 创建日期: 2026-05-17
> 状态: 🔄 构建进行中
> 前置: 工作区结构搭建完成

---

## 一、今日工作汇总

### 1.1 环境修复

- **问题**: Pi agent 的 bash 工具找不到 bash.exe（Git for Windows 安装于 E:\Git 而非标准路径）
- **解决**: 通过 MSYS2 路径映射 + 复制 bash.exe 到 `C:\Program Files\Git\bin\` 标准路径
- **状态**: ✅ bash 工具已可用

### 1.2 Rust 工作区

| Crate | 路径 | 状态 | 文件数 | 代码行 |
|-------|------|------|--------|--------|
| **hb-core** | `crates/core/` | ✅ 骨架完成 | 5 | ~300 |
| **hb-mono-bridge** | `crates/mono-bridge/` | ✅ 骨架完成 | 5 | ~250 |
| **hb-process-mgr** | `crates/process-mgr/` | ✅ 骨架完成 | 1 | ~150 |
| **hb-input-sim** | `crates/input-sim/` | ✅ 骨架完成 | 4 | ~300 |
| **hb-game-api** | `crates/game-api/` | ✅ 骨架完成 | 5 | ~300 |
| **hb-silverfish-core** | `crates/silverfish-core/` | ✅ 核心数据结构 | 12 | ~800 |
| **hb-silverfish-card-db** | `crates/silverfish-card-db/` | ✅ 占位 + build.rs | 2 | ~50 |
| **hb-silverfish-sim-cards** | `crates/silverfish-sim-cards/` | ✅ 模块结构 | 4 | ~80 |
| **hb-bot-framework** | `crates/bot-framework/` | ✅ 骨架完成 | 6 | ~300 |
| **hb-auth** | `crates/auth/` | ✅ 骨架完成 | 1 | ~50 |
| **hb-compression** | `crates/compression/` | ✅ 骨架完成 | 1 | ~50 |
| **hb-plugins** | `crates/plugins/` | ✅ 骨架完成 | 5 | ~150 |
| **hb-app** | `crates/app/` | ✅ 骨架完成 | 1 | ~100 |
| **合计** | **13 crates** | **✅** | **51** | **~2900** |

### 1.3 依赖链

```
hb-app (主入口)
├── hb-bot-framework
│   └── hb-core (配置/日志/Win32)
├── hb-game-api
│   └── hb-core + hb-input-sim + hb-mono-bridge
├── hb-silverfish-card-db
│   └── hb-silverfish-core
├── hb-silverfish-sim-cards
│   ├── hb-silverfish-core
│   └── hb-silverfish-card-db
├── hb-plugins (AutoStop/Monitor/Quest/Stats)
├── hb-auth
├── hb-compression
└── hb-process-mgr
    └── hb-core (windows crate)
```

### 1.4 缺失部件（待完成）

| 部件 | 说明 | 优先级 |
|------|------|--------|
| CardDB 完整枚举 | 从 C# CardDB 自动生成 3000+ 卡牌 ID | 🟡 中 |
| 1000+ 卡牌模拟 | 从 C# SimTemplate 生成 Rust 代码 | 🟡 中 |
| Mono 运行时偏移量 | 运行时检测炉石版本的 Mono 偏移 | 🔴 高 |
| 贝塞尔曲线参数 | 从原版 C# Client.cs 移植参数 | 🟡 中 |
| DialogHandler 策略 | 从 C# 策略模式移植 16 个处理器 | 🟡 中 |
| 认证协议 | 从 C# AuthClient/AuthServer 移植 | 🟢 低 |
| TUI 界面 | ratatui 实现的终端界面 | 🟢 低 |

## 二、构建状态

### 2.1 环境

| 组件 | 版本/状态 |
|------|----------|
| Rust | 1.94.1 (2026-03-25) |
| x86_64 target | ✅ 已安装 |
| i686 target | ✅ 已安装 |
| windows crate | 0.58.0 (编译中，体积较大) |

### 2.2 当前阻塞

`windows` crate (v0.58.0) 首次编译耗时较长（包含约 11 个 feature 模块 `Win32_Foundation`, `Win32_System_Memory`, `Win32_UI_WindowsAndMessaging` 等）。这是正常的首次构建行为，后续增量编译会大幅提速。

### 2.3 下一步构建策略

```
阶段 1: cargo check -p hb-silverfish-core   # 纯 AI 引擎（无 Windows 依赖）
阶段 2: cargo check -p hb-core              # 核心库 + Windows
阶段 3: cargo check (whole workspace)        # 全量检查
阶段 4: cargo build --target i686-pc-windows-msvc  # 32位交叉编译
```

## 三、后续行动

| 序号 | 行动 | 时间 |
|------|------|------|
| 1 | 完成首次 `cargo check` 全量编译 | 立即 |
| 2 | 修复编译错误（如有） | 编译后 |
| 3 | 将 C# CardDB.cardIDEnum 生成为 Rust 枚举 | 下一阶段 |
| 4 | 实现 `ProcessHandle::get_module_base` 完整逻辑 | 下一阶段 |
| 5 | 实现 `mono_runtime.rs` 的运行时检测扫描 | 下一阶段 |
