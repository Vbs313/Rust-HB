# 测试覆盖全面升级

本次提交为项目中 9 个 crate 添加了 **80 个新测试**，实现从 56 个到 136 个测试的全覆盖。
涵盖：AI 引擎所有模块、bot-framework(管理/插件/任务)、game-api(实体/场景/标签)、IPC序列化、键盘扫描码、进程管理、core 错误类型等。

## 新增测试分布
- silverfish-core: action(3) weapon(6) enemy_turn(5) ai(5) behavior(12) sim_template(6) → +37
- core: error(6) lib(2) → +8
- game-api: game_state(11) scene(2) → +13
- bot-framework: bot_mgr(7) plugin(5) task(3) → +15
- ipc: protocol serialization(9) → +9
- input-sim: keyboard scan codes(9) → +9
- process-mgr: rect/error(6) → +6
- 集成测试: 不变 (8)

## 修复
- MEMORY_BASIC_INFORMATION x86 适配（移除 PartitionId）
- extract_offsets 工具支持 4 种改名 exe
- MonoBridge/ProcessHandle/MonoRuntime 添加 dummy() 方法用于测试
- closehandle 空指针保护
- 添加 `Bots/` 目录到 .gitignore
