# Bot 框架串联 — AI 引擎集成完成

> 创建日期: 2026-05-18
> 状态: ✅ hb-app 主循环集成 DefaultRoutine，AI 引擎可调

---

## 变更内容

| 文件                                   | 变更                                                                            |
| -------------------------------------- | ------------------------------------------------------------------------------- |
| `bot-framework/src/default_routine.rs` | **新建**：DefaultRoutine 实现 Routine trait，创建 Playfield + 调用 Ai::do_all() |
| `bot-framework/src/lib.rs`             | 注册 `default_routine` 模块                                                     |
| `app/src/main.rs`                      | 注册 DefaultRoutine，主循环每 30 秒触发一次 AI 决策                             |

## 当前数据流

```
hb-app main_loop()
  ↓
BotManager.pulse()
  ↓ (每 30 秒)
RoutineManager.active().our_turn_logic()
  ↓
DefaultRoutine
  ├── 创建 Playfield（手牌+随从+英雄）
  ├── Ai::do_all() 搜索最佳动作
  │   ├── MoveGenerator 枚举动作
  │   ├── Playfield::do_action 模拟
  │   └── MiniSimulator DFS 搜索
  └── 输出最优 ActionType

编译: 0 errors ✅
```
