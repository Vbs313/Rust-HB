# Rust 工作区结构搭建完成

> 创建日期: 2026-05-17
> 状态: ✅ 完成
> 前置: 项目全景分析完成

---

## 一、工作区结构

```
E:/Next/
├── Cargo.toml                          # 工作区根（13 个 member crate）
├── .gitignore                          # Rust + 项目工程 gitignore
│
├── crates/
│   ├── core/                           # hb-core: 核心运行时
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── lib.rs                  # 项目根/数据目录
│   │       ├── config.rs               # 配置管理 (JSON/TOML 双格式)
│   │       ├── error.rs                # 错误类型定义
│   │       ├── log.rs                  # tracing 日志系统
│   │       └── win32.rs                # Win32 API 封装 (进程/内存)
│   │
│   ├── mono-bridge/                    # hb-mono-bridge: Mono 运行时桥接
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── lib.rs                  # MonoBridge 主入口
│   │       ├── mono_runtime.rs         # 运行时定位 (模式匹配)
│   │       ├── mono_image.rs           # 程序集映像
│   │       ├── mono_class.rs           # 运行时类实例读取
│   │       └── mappers.rs              # TAG 映射 + GameTag 枚举
│   │
│   ├── process-mgr/                    # hb-process-mgr: 进程管理
│   │   ├── Cargo.toml
│   │   └── src/lib.rs                  # 窗口发现/进程附件/窗口管理
│   │
│   ├── input-sim/                      # hb-input-sim: 输入模拟
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── lib.rs                  # InputSimulator 主入口
│   │       ├── bezier.rs               # 贝塞尔曲线路径生成
│   │       ├── mouse.rs                # SendInput 鼠标操作
│   │       └── keyboard.rs             # SendInput 键盘操作
│   │
│   ├── game-api/                       # hb-game-api: 游戏交互 API
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── lib.rs                  # GameApi 主入口
│   │       ├── game_state.rs           # 游戏状态表示 (Entity/Zone)
│   │       ├── game_actions.rs         # 游戏操作接口
│   │       ├── scene_detector.rs       # 场景检测
│   │       └── dialog_handlers.rs      # 策略模式弹窗处理
│   │
│   ├── silverfish-core/               # hb-silverfish-core: AI 引擎
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── lib.rs                  # 公共类型 + 模块声明
│   │       ├── playfield.rs            # 局面状态 (340+ 字段)
│   │       ├── minion.rs               # 随从数据结构
│   │       ├── weapon.rs               # 武器数据结构
│   │       ├── action.rs               # 动作系统 (actionEnum)
│   │       ├── ai.rs                   # AI 控制器
│   │       ├── move_generator.rs       # 动作生成器
│   │       ├── mini_simulator.rs       # DFS 搜索模拟器
│   │       ├── enemy_turn_simulator.rs # 敌方回合模拟
│   │       ├── behavior.rs             # 策略评估 (默认/快攻/控制)
│   │       ├── penality_manager.rs     # 惩罚值管理器
│   │       ├── card_db.rs              # 卡牌数据库
│   │       └── sim_template.rs         # 卡牌模拟模板
│   │
│   ├── bot-framework/                  # hb-bot-framework: Bot 框架
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── lib.rs                  # Bot/Plugin/Routine trait
│   │       ├── bot_manager.rs          # Bot 管理器
│   │       ├── plugin_manager.rs       # 插件管理器
│   │       ├── routine_manager.rs      # 策略管理器
│   │       ├── task_manager.rs         # tokio 任务管理器
│   │       └── events.rs              # 事件系统
│   │
│   ├── auth/                           # hb-auth: 认证模块
│   ├── compression/                    # hb-compression: 压缩模块
│   ├── plugins/                        # hb-plugins: 4 个内置插件
│   │   ├── auto_stop/monitor/quest/stats
│   └── app/                            # hb-app: 主程序入口
│       └── src/main.rs                 # tokio main + 初始化流程
│
├── 进展/                               # 项目进展文档
│   ├── 2026-05-17-0000-项目全景分析与重构总体规划.md
│   ├── 2026-05-17-0001-环境准备与Rust工具链安装.md
│   └── 2026-05-17-0002-Rust工作区结构搭建完成.md
│
└── setup_shell.js                      # 辅助脚本（修复 bash 环境）
```

## 二、各 Crate 代码统计

| Crate | 文件数 | 代码行数 | 关键模块 |
|-------|--------|---------|---------|
| hb-core | 5 | ~300 | 配置/日志/错误/Win32 |
| hb-mono-bridge | 5 | ~250 | 运行时/映像/类/映射 |
| hb-process-mgr | 1 | ~150 | 进程发现/窗口管理 |
| hb-input-sim | 4 | ~300 | 贝塞尔曲线/鼠标/键盘 |
| hb-game-api | 5 | ~300 | 状态/操作/场景/弹窗 |
| hb-silverfish-core | 12 | ~800 | AI 引擎核心 |
| hb-bot-framework | 6 | ~300 | Bot/Plugin/Routine 框架 |
| hb-auth | 1 | ~50 | 认证 |
| hb-compression | 1 | ~50 | 压缩 |
| hb-plugins | 5 | ~150 | 4 个内置插件 |
| hb-app | 1 | ~100 | 主入口 |
| **合计** | **46** | **~2750** | — |

## 三、与原版 C# 对比

| 对比项 | C# 原版 | Rust 新版 (当前进度) |
|--------|---------|-------------------|
| 项目数 | 2 (Origin + Backed) | 1 (workspace, 13 crates) |
| 文件数 | ~2860 .cs | 46 .rs |
| 代码行 | ~157,500 | ~2,750 (1.7%) |
| 构建系统 | MSBuild (.sln + .csproj) | Cargo (workspace) |
| 依赖管理 | NuGet + DLL | Cargo crates |
| UI | WPF (XAML) | TUI/Console (待实现) |
| AI 引擎 | Silverfish C# | hb-silverfish-core (骨架) |
| 卡牌模拟 | 1000+ SimTemplate | 待代码生成 |

## 四、已完成的设计决策

| 领域 | 决策 |
|------|------|
| Rust 目标 | `i686-pc-windows-msvc` (x86 32-bit) |
| Win32 API | `windows` crate |
| 异步运行时 | `tokio` |
| 日志 | `tracing` |
| 配置 | JSON + TOML 双格式 |
| 输入模拟 | `SendInput` API + 贝塞尔曲线 |
| AI 搜索 | 纯 Rust DFS (零外部依赖) |
| 卡牌生成 | 构建脚本从 C# CardDB 代码生成 |
| 错误处理 | `thiserror` + `anyhow` |

## 五、下一步

1. 修复 bash 工具 → 安装 Git for Windows 到标准路径
2. 运行 `cargo check` / `cargo build` 验证编译
3. 实现 mono_runtime.rs 的完整扫描逻辑
4. 实现 playfield.rs 的 do_action 完整逻辑
5. 代码生成：从 C# CardDB 生成 Rust 卡牌数据库
